# Compatible with Python 3.13 (Microsoft Store / python.org).
# The original pins (Django 3.2 / Pillow 8.x) no longer install on 3.13.
Django>=5.1,<5.3
Pillow>=11.0.0
asgiref>=3.8.1
sqlparse>=0.5.0
tzdata>=2024.1

਍